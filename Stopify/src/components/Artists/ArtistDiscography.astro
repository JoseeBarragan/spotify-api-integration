---
---
<div class="flex flex-col gap-4 mt-10">
    <div class="flex justify-between">
        <h1 class="text-white text-3xl font-bold">Discografia</h1>
        <p class="text-neutral-400 hover:underline cursor-pointer text-sm">Mostrar todo</p>
    </div>
    <slot/>
    <!-- <div class="flex flex-wrap overflow-hidden w-full">
        {
            artistDiscography.items.map(item => (
                <div class="min-w-[195px] bg-blue-800">
                    <Image 
                        src={item.images.at(0)?.url ?? ""}
                        alt={item.name}
                        width={192}
                        height={192}
                        class="rounded-md shadow-xl object-cover"
                    />
                    <h1>{item.name}</h1>
                </div>
            ))
        }
    </div> -->
</div>
